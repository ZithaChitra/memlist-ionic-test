import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.splashbrand.chitra',
  appName: 'lister test',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
